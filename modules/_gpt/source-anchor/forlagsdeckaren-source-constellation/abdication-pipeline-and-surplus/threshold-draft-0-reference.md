# threshold_draft_0

There is no standalone file by this name in the current packet.

For Del 10, `threshold_draft_0` should be treated as the gas receipt on which Erik writes the title after discovering the recording failure.

Status:

- not digitized
- sub-threshold artefact
- the draft before a file becomes a file

User-supplied anchors:

- written at the side of the E6
- title scrawled on a gas receipt
- not preserved as a real file
- remains below the threshold of publication

Normalization relevance for Del 10:

- names the transition from room-event to textual concept
- explains why the missing recording produces text rather than simply deleting it

Editorial implication:

Do not invent a fake `.txt` file for this.
Its non-file status is part of the architecture.
